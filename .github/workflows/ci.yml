name: CI

on:
  pull_request:
  push:
    branches:
      - main
      - develop
  workflow_dispatch:

permissions:
  contents: read
  pull-requests: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  GO_VERSION: "1.25.13"
  NODE_VERSION: "22"

jobs:
  go:
    name: Go
    uses: ./.github/workflows/reusable-go-ci.yml
    secrets: inherit
    with:
      go-version: "1.25.13"

  ui:
    name: UI
    uses: ./.github/workflows/reusable-ui-ci.yml
    secrets: inherit
    with:
      node-version: "22"

  repository:
    name: Repository
    uses: ./.github/workflows/reusable-repository-ci.yml
    with:
      node-version: "22"

  docker:
    name: Docker
    uses: ./.github/workflows/reusable-docker-ci.yml

  ci-success:
    name: CI Success
    runs-on: ubuntu-latest
    needs:
      - go
      - ui
      - repository
      - docker
    if: always()
    steps:
      - name: Check all jobs passed
        run: |
          if [ "${{ contains(needs.*.result, 'failure') }}" = "true" ]; then
            echo "Some checks failed"
            exit 1
          fi
          if [ "${{ contains(needs.*.result, 'cancelled') }}" = "true" ]; then
            echo "Some checks were cancelled"
            exit 1
          fi
          echo "All checks passed"

      - name: Summary
        run: |
          {
            echo "## CI Results"
            echo ""
            echo "| Area | Result |"
            echo "|------|--------|"
            echo "| Go | ${{ needs.go.result }} |"
            echo "| UI | ${{ needs.ui.result }} |"
            echo "| Repository | ${{ needs.repository.result }} |"
            echo "| Docker | ${{ needs.docker.result }} |"
          } >> "$GITHUB_STEP_SUMMARY"
