display_name: Telemetry Integration
short_description: Schema-safe telemetry extension across integration and assistant provider flows.
default_prompt: Use the telemetry-integration skill from a coordinator-attested approved plan. Respect assigned ownership, preserve metric keys and success/failure symmetry, exclude sensitive data, add required tests, run every declared command plus strict validation with --provider, and return evidence for independent code review; do not self-approve.
