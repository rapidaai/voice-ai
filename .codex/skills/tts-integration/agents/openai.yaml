display_name: TTS Integration
short_description: Transport-aware TTS provider integration with lifecycle and metric correctness.
default_prompt: Use the tts-integration skill from a coordinator-attested approved plan. Respect assigned ownership, preserve delta, done, interruption, audio, and latency contracts, add required tests, run every declared command plus strict validation with --provider, and return evidence for independent code review; do not self-approve.
