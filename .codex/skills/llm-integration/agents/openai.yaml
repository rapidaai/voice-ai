display_name: LLM Integration
short_description: Integration-api provider integration with caller factory and unified routing compatibility.
default_prompt: Use the llm-integration skill from a coordinator-attested approved plan. Respect assigned ownership, preserve caller, metric, audit, and compatibility contracts, add required tests, run every declared command plus strict validation with --provider, and return evidence for independent code review; do not self-approve.
