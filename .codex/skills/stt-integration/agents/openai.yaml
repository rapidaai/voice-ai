display_name: STT Integration
short_description: Transport-aware STT provider integration with packet contract enforcement.
default_prompt: Use the stt-integration skill from a coordinator-attested approved plan. Respect assigned ownership, preserve interim/final packet and latency contracts, add required tests, run every declared command plus strict validation with --provider, and return evidence for independent code review; do not self-approve.
