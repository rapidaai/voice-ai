roles 
id,
role_type, <Owner, Admin, Reader, Writer>
role_name,



permissions
id,
resource_type, <>
operation

role_permissions
role_id,
permission_id,
permission_title , eg : prompt_author, connection_credential_reader

user_roles
id,
role_id

user 
id,
ord_id,




Authentication
- authentication information
- user <> role and permissions <will do it later>

User
- profile information
- user <> notfication perference <will do later>
- user <> notification <will do later>


Organization
- Orgnization
- user <> organization <will do it later>



Project
- user <> project





eg:

# Permission 
# - Read, write, anything

# Resource
# - project
# - organization
# - etx

# Permission Group
# - Resource
# - Persmission


role -> permission group





-- role (1, 'author', 'custom_name_author')
-- permission(1, 'prompt', 'all')
-- permission(2, 'credential', 'reader')
[role_permission(1, 1, 'prompt_editor'), role_permission(1, 2, 'credential_reader')]
[user_roles(1, 1), user_roles(1, 2)]
user(1, 1);