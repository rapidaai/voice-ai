{
  "amendment": "03 - preserve infra boundary and single call-address ownership",
  "status": "accepted_candidate",
  "owner_approval": {
    "decision": "approved",
    "approved_at": "2026-09-03",
    "scope": "Review the implementation from the infra boundary and correct pattern-breaking ownership changes."
  },
  "problem_statement": "The first implementation copied every CallAddress field back from public middleware and duplicated the address in Session state. That breaks the existing thin-facade pattern and creates competing owners for immutable dialog identity.",
  "acceptance_criteria": [
    "Infra presents a defensive CallAddress snapshot to middleware.",
    "Only CallAddress.To, the route middleware-owned enrichment, is copied back to core.",
    "Middleware mutations to From, FromURI, ToURI, or Headers do not alter core-owned capture state.",
    "The accepted inbound configuration carries the enriched CallAddress into the dialog identity.",
    "Session does not store or expose a duplicate CallAddress.",
    "Application-ready, invite, session-established, and pipeline callbacks receive the dialog identity snapshot.",
    "Existing phone parsing, route resolution, outbound mapping, and metadata behavior remain unchanged."
  ],
  "allowed_paths": [
    "rfcs/0003-native-sip-assistant-phone-resolution.md",
    "rfcs/0003-native-sip-assistant-phone-resolution/jsons/",
    "api/assistant-api/sip/internal/core/",
    "api/assistant-api/sip/infra/",
    "api/assistant-api/sip/middleware/",
    "api/assistant-api/sip/pipeline/"
  ],
  "ownership": {
    "core": "Owns captured CallAddress fields and the accepted dialog identity.",
    "middleware": "May resolve and set only CallAddress.To.",
    "infra": "Creates a defensive middleware view and copies only To back to core.",
    "session": "Owns call lifecycle and runtime dependencies, not dialog address state.",
    "pipeline": "Consumes the callback CallAddress without consulting Session for address data."
  },
  "risks": [
    "A shallow Headers copy would still let middleware mutate core-owned data.",
    "Keeping Session CallAddress accessors would preserve duplicate ownership.",
    "Copying the whole middleware CallAddress back would allow accidental identity replacement."
  ],
  "required_tests": [
    "infra middleware conversion propagates CallAddress.To",
    "infra middleware conversion isolates From, FromURI, ToURI, and Headers",
    "inbound callbacks receive route-enriched To from accepted dialog identity",
    "outbound callbacks receive phone fields derived from authoritative outbound inputs",
    "pipeline maps callback phone values by direction"
  ],
  "required_commands": [
    "env GOCACHE=/private/tmp/voice-ai-gocache go test -count=1 ./api/assistant-api/sip/infra",
    "env GOCACHE=/private/tmp/voice-ai-gocache go test -count=1 ./api/assistant-api/sip/internal/core",
    "env GOCACHE=/private/tmp/voice-ai-gocache go test -count=1 ./api/assistant-api/sip/middleware",
    "env GOCACHE=/private/tmp/voice-ai-gocache go test -count=1 ./api/assistant-api/sip/pipeline",
    "git diff --check",
    "just agent-finalize \"api/assistant-api/sip/internal/core,api/assistant-api/sip/infra,api/assistant-api/sip/middleware,api/assistant-api/sip/pipeline\""
  ],
  "rollback": "Revert the amendment and correction commit together. No data migration is required.",
  "discussion": {
    "planner": "codex-coordinator",
    "challenger": "pending",
    "decision": "pending",
    "approved_by": "repository-owner",
    "open_blockers": []
  }
}
